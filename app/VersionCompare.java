import java.sql.*;

class VersionCompare {
	public static void main(String[] args) {
		
	}
	
	public static int compare(String v1, String v2) {
		Array v1Nums = v1.split(".");
		Array v2Nums = v2.split(".");
		for (int i = 0; i < Math.max(v1.length(), v2.length())) {
			if (arr[i])
		}
}