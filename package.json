{
  "name": "mzhserver",
  "version": "1.0.0",
  "description": "Mock Server",
  "main": "index.js",
  "scripts": {
    "start": "json-server --watch generator.js --routes router.json",
    "mock": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "mzh",
  "license": "ISC",
  "dependencies": {
    "faker": "^4.1.0",
    "lodash": "^4.17.15"
  },
  "devDependencies": {
    "json-server": "^0.15.0"
  }
}
